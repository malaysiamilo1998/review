import { useEffect, useState } from 'react'
import { getTopicsByParent } from '../../utils/sanity/sanity-utils'
import { useParams } from 'react-router-dom'

const Thread = () => {
  return <div>Thread list</div>
}

export default Thread
